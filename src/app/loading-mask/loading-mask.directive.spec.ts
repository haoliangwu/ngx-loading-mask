import { LoadingMaskDirective } from './loading-mask.directive';

describe('LoadingMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadingMaskDirective();
    expect(directive).toBeTruthy();
  });
});
